#include "main.h"

/**
 * _abs -to  compute absolute value of n
 * @n: int to compute as absolute
 * Return: absolute value from _abs
 *
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
