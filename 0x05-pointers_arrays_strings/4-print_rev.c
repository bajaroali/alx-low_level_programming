#include "main.h"

/**
 * print_rev - print  a string in reverse, followed by a\n
 * @s: string to be printed
 */
void print_rev(char *s)
{
	int j, k, len;

	j = 0;

	while (s[j] != '\0')
	{
		i++;
	}

	len = j;

	for (k = len - 1; k >= 0; k--)
	{
		_putchar(s[k]);
	}

	_putchar('\n');
}
